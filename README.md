# W1l7dev's dotfiles

Those are my dotfiles. I am a windows user, so I use [WSL](https://docs.microsoft.com/en-us/windows/wsl/install-win10) to run a linux environment. The files should work with MacOS and Linux too, but this repository is axed on WSL and Windows.

## TODO

- [ ] Add an installation script for the dotfiles

## Installation

```bash
git clone https://github.com/W1l7dev/dotfiles.git %USERPROFILE%/.dotfiles
cd %USERPROFILE%/.dotfiles
```

> Note: For now the installation script is not ready, so you have to manually copy the files to the right place.
